VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key

# Admin key used to authorize destructive actions from the client.
# WARNING: VITE_ envs are exposed to the browser. For production, prefer server-side endpoints.
VITE_ADMIN_KEY=KOBINA2025ADMIN

# Do NOT commit real service role keys. Use server-only envs for SUPABASE_SERVICE_ROLE_KEY
# SUPABASE_SERVICE_ROLE_KEY=service-role-key
